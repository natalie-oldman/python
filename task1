
from sys import argv

def salary():
    try:
        hours, salary, bonus = map(float, argv[1:])
        sum = round(hours * salary + bonus, 2)
        print(f'Заработная плата сотрудника - {sum}')
    except ValueError:
        print('Нужно ввести 3 числа.')

salary()
